#!/bin/sh
## GIT, VIM
# update base system and install a few extras
sudo apt-get update && sudo apt-get upgrade -y && sudo apt-get install -y ntp ntpdate git vim-nox

## GIT
# name = kedwards
# email = kedwards@kncedwards.com
echo -e '[user]\n\tname = Kevin Edwards\n\temail = kedwards@kncedwards.com' | tee ~/.gitconfig > /dev/null
